<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.AppCompat.DayNight" parent="Theme.AppCompat" />

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/night_primary</item>
        <item name="colorPrimaryDark">@color/night_primary</item>
        <item name="colorAccent">@color/night_primary</item>
        <item name="android:editTextBackground">@android:color/transparent</item>
        <item name="android:windowBackground">@color/night_black</item>
        <item name="android:colorBackground">@color/night_black</item>
        <item name="colorBackgroundFloating">@color/night_gray</item>
        <item name="android:colorBackgroundFloating" tools:targetApi="23">@color/night_gray</item>
    </style>

</resources>
